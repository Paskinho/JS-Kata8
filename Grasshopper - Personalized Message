function greet (name, owner) {
  if (name == owner) {
    return 'Hello boss'
  }
  else return 'Hello guest'
}

Три равно

Можно через условный оператора ?


function greet (name, owner) {
  return name === owner ? 'Hello boss' :  'Hello guest';
}
