Моё решение (оно же правильное)

function litres(time) {
  let n = time * 0.5
  if (time > 0) {
    return Math.floor(n) 
  }
  else return 0;
}


решение в одну строку:


function litres(time) {
  return Math.floor(time * 0.5);
}
