var summation = function (num) {
  return num * (num + 1) /2
  }
  чистая математика
  
  
  самое популярное решение 
  
  var summation = function (num) {
  var sum = 0;
  for(var i=0; i<=num;i++){
    sum+=i
  }
  return sum;
}
