моё решение 
function numberToString(num) {
  if (Number.isInteger(num)) {
    return 'num'
  }
  else return 'error'
  }
  
  Решение простейшее
  
  function numberToString(num) {
 return num.toString()
  }
  
  .tostring перевод в строку)
