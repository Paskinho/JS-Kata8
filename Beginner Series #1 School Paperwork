function paperwork(n, m) {
  if (n>0 && m>0) {
    return n * m
  }
  else return 0
}

Через логический оператор и. Два условия обязательные

Через логический оператор ? код будет меньше 

function paperwork(n, m) {
  return n > 0 && m > 0 ? n * m : 0
}
